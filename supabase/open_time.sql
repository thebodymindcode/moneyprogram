-- Поле «во сколько открыть день» для админ-панели.
-- Выполнить ОДИН РАЗ в Supabase → SQL Editor. Безопасно, ничего не удаляет.
-- Хранит время открытия дня в формате "ЧЧ:ММ" по Москве (например 13:00). Пусто = день открывается в 5:00 по умолчанию.

alter table public.days add column if not exists open_time text;
