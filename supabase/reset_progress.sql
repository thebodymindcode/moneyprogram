-- ============================================================
--  Протокол денег, СБРОС ЛИЧНОГО ПРОГРЕССА у ВСЕХ участников
--  Перед официальным стартом 1 июля. Запусти один раз:
--  Supabase → SQL Editor → вставь этот файл → Run.
--
--  ВАЖНО: контент НЕ трогаем. Дни и задания (public.days, public.tasks),
--  список доступа (allowed_emails), админы и профили остаются как есть.
--  Стираем только то, что человек прошёл и наотвечал, чтобы 1 июля
--  все начали с чистого листа.
-- ============================================================

-- пройденные дни (галочки «день пройден»)
delete from public.progress;

-- ответы на задания и галочки выполнения заданий
delete from public.task_answers;

-- заметки дня (личный дневник по дням)
delete from public.notes;

-- отметки состояния «тревога ↔ спокойствие» по дням
-- (на устройстве у человека кеш может остаться, но в базе чисто)
delete from public.checkins;

-- Готово. Контент уроков на месте, личный прогресс обнулён для всех.
-- Если заметки и отметки состояния хочешь СОХРАНИТЬ, убери две последние
-- строки (notes и checkins) перед запуском.
