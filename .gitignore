# Секреты и локальная конфигурация
.env
.env.*
# config.js публикуется намеренно: в нём только публичный (publishable) ключ
# Supabase, безопасный на фронтенде, данные защищает RLS.

# Зависимости и сборка (на будущее, при переходе на Vite)
node_modules/
dist/
.vite/

# Отчёты проверки site-check
_check/

# Системное
.DS_Store
